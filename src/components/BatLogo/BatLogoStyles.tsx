import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    gap: 20
  },
  title: {
    color: '#f8f410',
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 30
  }
});